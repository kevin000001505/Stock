services:
  stock-app:
    container_name: stock-app-latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8502:8501"
    restart: unless-stopped
    volumes:
      - .:/app
      - ./data:/app/data